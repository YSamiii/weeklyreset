# Weekly Reset iPad v22

- 修复 App 跨日后仍显示前一天训练的问题。
- App 从后台恢复、页面重新显示或重新获得焦点时，会按多伦多日期检查是否进入新的一天。
- 跨过午夜且 App 一直保持前台时，也会在一分钟内自动切换到今天。
- 日期变化后自动回到“今日”页面，并载入当天训练；跨周时自动切换到新的本周计划。
- 设置页新增“导出完整 JSON 备份”和“导入 JSON 备份”。
- iPad 导出优先调用系统分享面板，可选择“存储到文件”、AirDrop 或其他分享方式；不支持文件分享时自动回退为下载。
- 备份包含视频库、多周计划、训练打卡、生理期设置、频道排除和个人偏好。
